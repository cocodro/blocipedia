<div class="row">
  <div class="col-md-8"><h1><%= @wiki.title %></h1></div>
  <div class="col-md-4">
    <h1><ul class="nav nav-pills">
      <% if policy(@wiki).edit? %>
        <%= link_to "Add Section", new_wiki_section_path(@wiki, @section), class: 'btn btn-success' %>
        <%= link_to "Edit Wiki", edit_wiki_path(@wiki), class: 'btn btn-success' %>
      <% end %>
      <% if policy(@wiki).destroy? %>
        <%= link_to "Delete Wiki", @wiki, method: :delete, remote: true, class: 'btn btn-danger', data:{confirm: "Are you sure? The wiki will be permanently deleted."} %>
      <% end %>
    </ul></h1>
  </div>
</div>
</br>
<div class="media-body">
  <%= render partial: 'sections/section', collection: @sections %>
</div>
